---
import type { NavListType } from "@types";
import NavListItem from "./NavListItem.astro";

type Props = NavListType;

const { items } = Astro.props as Props;
---

<ul>
    {items.map((item) => <NavListItem {...item} />)}
</ul>
