---
import type { MetaTags } from "../types/meta-tags";
import Layout from "../layouts/Layout.astro";
import H1 from "../components/H1.astro";

const MetaTags: MetaTags = {
    title: "Home"
};
---

<Layout MetaTags={MetaTags}>
    <H1>Digital Bank Landing Page</H1>
</Layout>
